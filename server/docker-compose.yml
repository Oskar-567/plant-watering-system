services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: plantdb
      POSTGRES_USER: plant
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
